{
  "name": "extraccion-backend",
  "version": "1.0.0",
  "description": "Backend del sistema de extracción",
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm install",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": "18.x"
  }
} 